# coding=utf-8


# proxy
# here is  abuyun proxy
# and you can use the other proxy

proxy_pro = {
        "http": "xxxxx",
        "https": "xxxxx",
    }

proxy_dyn = {
        "http": "xxxxx",
        "https": "xxxxx",
    }

# retry time

retry = 5

# sleep time
# when connect error
# and proxy is use
# the sleep time is about 3~5

sleep_w = [3, 4, 5]

# when finish a request and then
# need a sleep time just about 1-3

sleep_q = [1, 2, 3]

# request timeout
# seconds
timeout = 15